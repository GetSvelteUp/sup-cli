name: test-github-actions
on: [push]
jobs:
  build:
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
      - run: npm install -g pnpm
      - run: pnpm install
      - run: pnpm run build
